name: Deploy

# Runs terraform plan + apply for the target environment.
#
# Triggers:
#   - push to main    -> automatically deploys to dev
#   - workflow_dispatch -> manually deploys to staging or prod
#
# Promotion path: dev (auto) -> staging (manual) -> prod (manual + approval)
#
# GitHub Environment protection rules enforce the gates:
#   dev     - no protection, auto-deploys on every merge to main
#   staging - required reviewers before the job runs
#   prod    - required reviewers + deployment protection rules
#
# AWS authentication uses OIDC (no long-lived access keys stored anywhere).
# Each environment has a dedicated IAM role: edp-{env}-github-actions-role
#
# Required GitHub configuration:
#   Environment variables (Settings -> Environments -> {env} -> Variables):
#     AWS_ACCOUNT_ID - the 12-digit AWS account ID
#   Environment secrets (Settings -> Environments -> {env} -> Secrets):
#     TF_VAR_DB_PASSWORD             - RDS master password
#     TF_VAR_REDSHIFT_ADMIN_PASSWORD - Redshift admin password

on:
  push:
    branches: [main]
    paths:
      - 'environments/**'
      - 'modules/**'
      - '.github/workflows/deploy.yml'

  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: choice
        options: [staging, prod]

jobs:
  deploy:
    name: Deploy to ${{ inputs.environment || 'dev' }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'dev' }}

    permissions:
      id-token: write
      contents: read

    env:
      TF_ENV: ${{ inputs.environment || 'dev' }}

    defaults:
      run:
        working-directory: environments/${{ inputs.environment || 'dev' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.6"

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/edp-${{ env.TF_ENV }}-github-actions-role
          aws-region: eu-central-1

      - name: Fetch MWAA artefacts from S3
        # The orchestration module calls filemd5() on requirements.txt and plugins.zip
        # at plan time. Downloading from S3 ensures the MD5 matches what is already
        # in Terraform state, so Terraform sees no change to those resources and
        # does not trigger a 35-minute MWAA environment update on every deploy.
        #
        # On a fresh session the bucket may not exist yet or the files may not have
        # been uploaded. In that case the download is skipped and empty placeholder
        # files are written instead. Terraform will upload them when it creates the
        # MWAA environment for the first time, and the real files will be synced by
        # the platform-orchestration-mwaa-airflow deploy workflow shortly after.
        working-directory: ${{ github.workspace }}
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          DEST="${GITHUB_WORKSPACE}/../platform-orchestration-mwaa-airflow"
          mkdir -p "${DEST}"
          BUCKET="edp-${{ env.TF_ENV }}-${ACCOUNT_ID}-mwaa-dags"

          if aws s3 cp "s3://${BUCKET}/requirements.txt" "${DEST}/requirements.txt" 2>/dev/null; then
            echo "requirements.txt fetched from S3."
          else
            echo "requirements.txt not found in S3 (fresh deploy). Using empty placeholder."
            touch "${DEST}/requirements.txt"
          fi

          if aws s3 cp "s3://${BUCKET}/plugins.zip" "${DEST}/plugins.zip" 2>/dev/null; then
            echo "plugins.zip fetched from S3."
          else
            echo "plugins.zip not found in S3 (fresh deploy). Creating minimal placeholder."
            python3 -c "import zipfile; zipfile.ZipFile('${DEST}/plugins.zip', 'w').close()"
          fi

      # The profile setting in backend.tf is ignored in CI because AWS env vars
      # set by configure-aws-credentials take precedence over named profiles.
      - name: terraform init
        run: terraform init -backend-config="profile=" -reconfigure

      - name: terraform plan
        id: plan
        run: |
          set -o pipefail
          terraform plan -no-color -input=false -out=tfplan 2>&1 | tee /tmp/plan.txt
        env:
          TF_VAR_db_password: ${{ secrets.TF_VAR_DB_PASSWORD }}
          TF_VAR_redshift_admin_password: ${{ secrets.TF_VAR_REDSHIFT_ADMIN_PASSWORD }}
          TF_VAR_profile: ""

      - name: Write plan to job summary
        working-directory: ${{ github.workspace }}
        run: |
          echo "### Terraform Plan (${{ env.TF_ENV }})" >> "$GITHUB_STEP_SUMMARY"
          echo '```hcl' >> "$GITHUB_STEP_SUMMARY"
          cat /tmp/plan.txt >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: terraform apply
        run: terraform apply -auto-approve -input=false tfplan

      - name: Deployment summary
        working-directory: ${{ github.workspace }}
        run: |
          echo ""
          echo "Terraform apply complete: edp-${{ env.TF_ENV }}"
          echo "Environment: ${{ env.TF_ENV }}"
          echo "Commit: ${{ github.sha }}"
